# yaml-language-server: $schema=none
healthcheck:
  test: ["CMD", "node", "-e", "fetch('http://localhost:8055/server/ping').then(r => r.ok ? process.exit(0) : process.exit(1)).catch(() => process.exit(1))"]
  interval: 10s
  timeout: 5s
  retries: 5
  start_interval: 5s
  start_period: 30s